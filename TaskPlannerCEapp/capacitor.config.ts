import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.TaskPlannerCE.app',
  appName: 'TaskPlannerCE',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
