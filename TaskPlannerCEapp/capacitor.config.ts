import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.TaskPlannerCE.app',
  appName: 'TP',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
